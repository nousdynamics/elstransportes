{
  "name": "els-transportes",
  "version": "1.0.0",
  "private": true,
  "description": "Site institucional da ELS Transportes — Cloudflare Workers (Static Assets).",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
